// API Configuration for RINVO
// Update this after deploying backend to Railway

// Use a relative API base so the frontend can call the backend at the same origin
// When deploying backend to a separate host, replace with that full URL.
const API_BASE_URL = '/api';

// Example for deployed backend (uncomment and set your URL):
// const API_BASE_URL = 'https://your-backend.example.com/api';

export default API_BASE_URL;

// Instructions:
// 1. Deploy backend to Railway
// 2. Get your Railway URL (e.g., https://rinvo-backend-production.up.railway.app)
// 3. Replace localhost:3000 with your Railway URL
// 4. Redeploy to Firebase
// API Configuration for RINVO
// Update this after deploying backend to Railway

// Use a relative API base so the frontend can call the backend at the same origin
// When deploying backend to a separate host, replace with that full URL.
const API_BASE_URL = '/api';

// Example for deployed backend (uncomment and set your URL):
// const API_BASE_URL = 'https://your-backend.example.com/api';

export default API_BASE_URL;

// Instructions:
// 1. Deploy backend to Railway
// 2. Get your Railway URL (e.g., https://rinvo-backend-production.up.railway.app)
// 3. Replace localhost:3000 with your Railway URL
// 4. Redeploy to Firebase
